<?xml version="1.0" encoding="utf-8"?>
<!--
  Filename: AndroidManifest.xml
  FullPath: /home/u2re-dev/U2RE.space/apps/CWSP-reborn/app/android/AndroidManifest.xml
  Change date and time: 16.30.00_10.07.2026
  Reason for changes: Pass-II fill: minimal Capacitor Android manifest for the CWSP shell (minimal/network/settings/airpad views).

  NOTE: `app/android/main/AndroidManifest.xml` symlinks here, so this single
  manifest is the canonical one for the Android projection.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Clipboard / share hooks -->
    <uses-permission android:name="android.permission.READ_CLIPBOARD" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_CLIPBOARD" tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="false"
        android:label="CWSP"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${cwspAllowCleartext}"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        tools:targetApi="34">

        <activity
            android:name="space.u2re.cwsp.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
