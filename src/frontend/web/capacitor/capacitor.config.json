{
  "appId": "space.u2re.cwsp",
  "appName": "CWSP Transfer",
  "webDir": "../../build/capacitor/web",
  "android": {
    "path": "app/android"
  },
  "plugins": {
    "SystemBars": {
      "style": "DARK",
      "insetsHandling": "disable"
    }
  },
  "server": {
    "androidScheme": "https",
    "cleartext": false
  },
  "_comment": "Runtime-safe JSON (no ${env} placeholders). Sync copies this into assets/capacitor.config.json. TS config remains the env-aware source for tooling."
}
