{
  "appId": "space.u2re.cwsp",
  "appName": "CWSP",
  "webDir": "../../build/capacitor",
  "android": {
    "path": "app/android",
    "buildOptions": {
      "keystorePath": "${CWSP_ANDROID_KEYSTORE}",
      "keystoreAlias": "${CWSP_ANDROID_KEY_ALIAS}"
    }
  },
  "server": {
    "androidScheme": "${CWSP_ANDROID_SCHEME:https}",
    "cleartext": false
  },
  "_comment": "webDir is relative to this config file (src/frontend/web/capacitor/). Run Capacitor CLI with --config pointing here. JSON mirrors capacitor.config.ts; env placeholders must be substituted by tooling before use."
}
